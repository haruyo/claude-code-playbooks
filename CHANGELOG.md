# 変更履歴（Changelog）

このリポジトリ（[claude-code-playbooks](https://github.com/haruyo/claude-code-playbooks)）の作業履歴です。手順書の追加・更新のたびに、新しい日付の見出しを上に足していきます。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠します（`追加` / `変更` / `修正` / `削除`）。

---

## 2026-06-01

### 追加
- 手順書3本目 **「AI検索対策 診断レポート構築手順（GEO / AIO 診断）」** を追加。ChatGPT・Google AI Overviews / AI Mode・Perplexity などの「AIの回答」に自社サイトが引用されやすいかを診断するプレイブック。**「月次アクセスレポート構築手順」を実施済みのお客様向け**で、既存の認証（`token.json`）と GA4 / Search Console データをそのまま再利用するため追加費用はかからない。
- `README.md` と `zenn-index.md` の手順書一覧テーブルに1行追記。

### AI検索対策 診断レポート構築手順 の設計
- 診断は **4領域**：①クロール許可（AIクローラーが robots.txt/WAF で弾かれていないか。**最優先**）／②コンテンツの抽出しやすさ（見出し直後の結論・冒頭の定義・FAQ・最終更新日）／③構造化データ・運営者情報（E-E-A-T）／④計測（GA4でのAI流入可視化・質問型クエリ傾向）。
- 進め方は **STEP 0 前提確認 → STEP 1 自動データ収集（`diagnose_ai.py`：robots.txt 判定・主要ページの JSON-LD/H2/更新日点検・llms.txt 有無・GA4 のAI流入抽出）→ STEP 2 Claude による目視併用の診断 → STEP 3 `ai_search_diagnosis_YYYYMM.md` 生成 → STEP 4 優先対策トップ5の案内**。
- **スタンスは「両論併記＋基本優先」**。Google公式の「AI検索対策＝従来のSEO、llms.txt 等の専用施策は不要」という見解を出典付きで明示し、効果が不確実な施策は「**任意・様子見**」と区別。中小企業に無駄な工数をかけさせない配慮。
- **情報鮮度への対応**：冒頭に判断基準日（2026-05-31）を明記し、`ai_config.py` の `GUIDE_INFO_DATE` と連動。**3〜6ヶ月ごとに `WebSearch` で最新化する「メンテナンス手順」** を本文に内蔵（クローラー名・推奨スキーマ・GA4正規表現・Google公式見解を取り直す）。診断レポートにも診断日と情報基準日を両方記載する。
- 付録に **robots.txt 推奨テンプレ（2パターン）／JSON-LD 最小例（Organization・FAQPage）／GA4 のAI流入正規表現** を同梱。`Google-Extended` をブロックしても `Googlebot` は止めない注意も明記。

---

## 2026-05-30（その3）

### 修正
- 費用に関する表現を見直し。「無料の範囲で動く」だけだと誤解を招くため、**Claude Code 自体の利用料（サブスクリプション／API 従量課金）はかかる**ことと、**つなぐ外部サービスは無料枠内**であることを分けて明記（`README.md` および各手順書の「はじめに」）。
- 「取り扱いの注意」で、認証情報（`token.json` / `client_secret.json` 等）が**全手順書で作られるわけではない**点を反映し、「手順書によっては作られることがある／作られた場合は秘密情報として扱う」という条件付きの表現に修正（`README.md`）。

---

## 2026-05-30（その2）

### 追加
- 手順書2本目 **「セキュリティレベル診断手順」** を追加。お客様が自分で作ったプログラムについて、「どれだけセキュリティを気にすべきか」を **10段階** で判定するプレイブック。
- `README.md` の手順書一覧テーブルに1行追記。

### セキュリティレベル診断手順 の設計
- 包括的なセキュリティ監査ではなく **「どこまで気にすべきかの仕分け」** に役割を限定。深い脆弱性点検が必要なレベルでは Claude Code 標準の **`/security-review`** へ案内する方針を明記。
- 進め方は **コードの自動分析（STEP 1）→ 不足情報のヒアリング（STEP 2）→ 点数化（STEP 3）→ レベル別の最低限対策（STEP 4）→ 結果ファイル保存（STEP 5）** の流れ。
- 判定ロジックは **6観点（A.データ機密性／B.公開度／C.外部入力／D.決済／E.影響範囲／F.責任）を最大20点で配点 → 10段階に換算**。さらに「認証情報・カードを扱う」「公開×個人情報」「決済」「本番操作」は最低レベルを底上げする **引き上げルール** を併用。
- レベルを **4つの危険帯（🟢1–2／🟡3–4／🟠5–7／🔴8–10）** に分け、帯ごとに最低限やることを提示（下の帯は上の帯を内包）。レベル5以上で `/security-review` を案内。
- 診断結果を `security_診断結果_YYYYMMDD.md` に保存。秘密情報（実際のキー等）は記録しない方針を明記。

---

## 2026-05-30

### 追加
- リポジトリを新規作成し、公開リポジトリ `claude-code-playbooks` として初版を公開。
- `README.md`：シリーズのインデックス（使い方・手順書一覧テーブル・相談窓口・取り扱い注意・ライセンス）。今後は手順書を追加するたびに一覧テーブルへ1行追記する運用。
- `LICENSE`：MIT License。
- `.gitignore`：認証情報（`client_secret.json` / `token.json`）と、各お客様環境で生成される作業ファイル（`config.py` / `ga4_data.json` / `sc_data.json` / `profile.md` / `blog_ideas_*.md` / `annual_report_*.md`）を除外。
- 手順書1本目 **「月次アクセスレポート構築手順」** を追加（Google Analytics 4 + Search Console → スプレッドシートの月次レポートを Claude Code が構築）。

### 月次アクセスレポート構築手順 の主な整備内容
- 冒頭に「はじめに（前提と方針）」を新設。Claude Code 向けの手順書であることを明記し、費用がかからない点（無料枠内・実行回数による従量課金なし）と相談窓口 <https://onoharyo.com> を案内。
- 接続完了後の **「つながった後にできること（活用メニュー）」** を新設。お客様の指示がざっくりしていても、Claude が決まったフローで進められる3メニューを用意：
  1. 流入キーワードを伸ばすブログ案
  2. トレンド × 事業内容で新規キーワード・新記事を提案
  3. この1年の包括レポート
  - 前提として事業プロフィール `profile.md` を登録するフロー（サイトURLから Claude が草案を作り、お客様の確認を取る）も追加。
- 完成したスプレッドシートの **URL を必ず提示** するよう、スクリプト出力（`REPORT_URL>>` 行）と STEP 7・STEP 8・毎月の運用・トラブルシューティングに明記。
- STEP 4 に **「コードの扱い方」** を追加。掲載コードは動作実績のあるベースとし、ライブラリ/API の仕様変更で動かない場合は最新の公式情報を確認して必要最小限の改修を行う方針（設計＝取得項目・出力構造は固定）を明文化。トラブルシューティングにも版差エラーの項目を追加。
- ファイル名を `お客様向け_月次レポート構築手順 (1).md` → `月次アクセスレポート構築手順.md` にリネーム。

---

<!-- 新しい作業をしたら、この上に「## YYYY-MM-DD」の見出しで追記してください。 -->
